from django.urls import path
from place import views as v

urlpatterns = [
    # path('create/', v.PlaceCreateView.as_view(), name='create'),
    # path('detail/<pk>/', v.PlaceDetailView.as_view(), name='detail'),
    # path('list/', v.PlaceListView.as_view(), name='list'),
    # path('update/<pk>/', v.PlaceUpdateView.as_view(), name='update'),
    # path('delete/<pk>/', v.PlaceDeleteView.as_view(), name='delete'),
]
